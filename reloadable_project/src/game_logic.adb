with Ada.Text_IO;

package body Game_Logic is

  procedure process (UpdateDelta : Duration; ET : Entity_Table_Access) is
  begin
    Ada.Text_IO.Put_Line("_NOT_ alive");
    --Ada.Text_IO.Put_Line("It's alive!");
  end process;

end Game_Logic;
